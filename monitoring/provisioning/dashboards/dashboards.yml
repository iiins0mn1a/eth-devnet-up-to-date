apiVersion: 1

providers:
  - name: "Consensus Dashboards"
    orgId: 1
    folder: "Ethereum PoS"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: "/etc/grafana/provisioning/dashboards-json"

